interface:
  display_name: "Dynamic Subagent Router"
  short_description: "Select agents by workload, capability, and cost"
  default_prompt: "Use $route-subagents to derive W and R, apply the static fitness index, spawn the selected route, and verify the result."
