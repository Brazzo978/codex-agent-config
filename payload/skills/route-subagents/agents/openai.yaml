interface:
  display_name: "Dynamic Subagent Router"
  short_description: "Route agents by task fit, capability, and cost"
  default_prompt: "Use $route-subagents to apply workload fit and the dated intelligence/API-cost table, choose the cheapest sufficient profile, protect my usage budget, delegate, and verify."
