interface:
  display_name: "Dynamic Subagent Router"
  short_description: "Route agents with simple examples first"
  default_prompt: "Use $route-subagents to match a plain-language example first, use the static table only if needed, label the task, spawn it, and verify the result."
